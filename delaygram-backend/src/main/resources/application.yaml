
spring:
  datasource:
    url: jdbc:h2:file:./data/delaygram;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: sa
    password: password
    initialization-mode: always
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  jackson:
    mapper:
      default-view-inclusion: true
